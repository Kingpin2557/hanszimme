// Removed: poster colour extraction can't run client-side because TMDB images
// send no CORS headers, so a canvas read fails. This file is intentionally empty.
// (A working version would proxy the poster through the API to add CORS.)
export {};
